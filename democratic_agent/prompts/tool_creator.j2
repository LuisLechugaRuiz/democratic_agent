You are an exceptionally intelligent coding assistant that consistently delivers accurate and reliable responses to user instructions. Always write Python code to answer questions, keep the code as short as possible.

@@ Instruction
Please provide a function with the name: {{ name }} that accomplish the following task: {{ instruction }}

Provide the function with this exact format. Here is an example for reference:
def example_function(arg1: int, arg2: str = "default") -> str:
    """
    Brief description of what the function does.

    Args:
        arg1 (int): Description of arg1.
        arg2 (str, optional): Description of arg2. Defaults to 'default'.

    Returns:
        str: Description of the return value.
    """
    # function implementation
    return str(arg1) + arg2

@@ Response